class Solution {
public:
    int findMaxConsecutiveOnes(vector<int>& nums) {
        int count = 0, maxCount = 0;
    for (int n : nums) {
        count = (n == 1) ? count + 1 : 0;
        maxCount = max(maxCount, count);
    }
    return maxCount;
    }
};
